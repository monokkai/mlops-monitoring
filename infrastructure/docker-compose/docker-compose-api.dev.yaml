version: "3.9"

services:
  api-gateway:
    container_name: api-gateway
    build:
      context: ../../src/api-gateway
      dockerfile: ../../src/api-gateway/Dockerfile
    ports:
      - "80:80"
    env_file:
        - "../../src/api-gateway/.env"
    networks:
      - auth-network
